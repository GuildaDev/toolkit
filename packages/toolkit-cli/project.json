{
  "name": "toolkit-cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/toolkit-cli/src",
  "projectType": "library",
  "targets": {
    "bundler": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/toolkit-cli",
        "main": "packages/toolkit-cli/src/index.ts",
        "tsConfig": "packages/toolkit-cli/tsconfig.lib.json",
        "assets": [
          "packages/toolkit-cli/*.md"
        ],
        "generatePackageJson": true,
        "format": [
          "esm",
          "cjs"
        ]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/toolkit-cli/tsconfig.lib.json --emitDeclarationOnly --outDir ./dist"
      },
      "dependsOn": [
        {
          "projects": "self",
          "target": "bundler"
        }
      ]
    },
    "publish:build:dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd dist/packages/toolkit-cli && npm publish --registry http://localhost:4873"
      },
      "dependsOn": [
        {
          "projects": "self",
          "target": "build"
        }
      ]
    },
    "publish:build:prod": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd dist/packages/toolkit-cli && npm publish --access=public --registry https://registry.npmjs.org"
      },
      "dependsOn": [
        {
          "projects": "self",
          "target": "build"
        }
      ]
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/toolkit-cli/**/*.ts",
          "packages/toolkit-cli/package.json"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/toolkit-cli/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}